<UserControl x:Class="PrintPilotProxy.App.Views.ProxySettingsPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- ProxySettingsPage is superseded by NetworkSettingsPage.
         This stub prevents navigation errors if old bookmarks are used. -->
    <StackPanel>
        <TextBlock Text="Proxy Settings" Style="{StaticResource HeaderTextBlockStyle}" />
        <TextBlock Text="This page has moved to Network Settings." Foreground="{StaticResource WarningBrush}" />
    </StackPanel>
</UserControl>
