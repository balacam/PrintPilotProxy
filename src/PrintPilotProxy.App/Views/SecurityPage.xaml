<UserControl x:Class="PrintPilotProxy.App.Views.SecurityPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:loc="clr-namespace:PrintPilotProxy.App.Localization">
    <StackPanel>
        <TextBlock Text="{loc:Loc Key=Page.Security}" Style="{StaticResource HeaderTextBlockStyle}" />

        <Border Style="{StaticResource CardStyle}" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="{loc:Loc Key=Sec.Title}"
                           Style="{StaticResource SubheaderTextBlockStyle}" />
                <TextBlock Text="{loc:Loc Key=Sec.Description}"
                           Foreground="{StaticResource TextBrush}" Opacity="0.7"
                           Margin="0,4,0,12" TextWrapping="Wrap" />

                <!-- Overall Status -->
                <Border Background="{StaticResource PanelBackgroundBrush}" CornerRadius="8"
                        Padding="16,10" Margin="0,0,0,12">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="🛡️" FontSize="18" Margin="0,0,10,0" VerticalAlignment="Center" />
                        <TextBlock Text="{Binding OverallStatus}"
                                   FontSize="14" FontWeight="SemiBold"
                                   VerticalAlignment="Center">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="{StaticResource TextBrush}" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding OverallStatusBrushKey}" Value="SuccessBrush">
                                            <Setter Property="Foreground" Value="{StaticResource SuccessBrush}" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding OverallStatusBrushKey}" Value="ErrorBrush">
                                            <Setter Property="Foreground" Value="{StaticResource ErrorBrush}" />
                                        </DataTrigger>
                                        <DataTrigger Binding="{Binding OverallStatusBrushKey}" Value="WarningBrush">
                                            <Setter Property="Foreground" Value="{StaticResource WarningBrush}" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Border>

        <!-- Re-run Button -->
        <Button Content="{loc:Loc Key=Sec.RerunAudit}"
                Command="{Binding RunAuditCommand}"
                Padding="16,8" Margin="0,0,0,12" />

        <!-- Loading Indicator -->
        <TextBlock Text="{loc:Loc Key=Diag.Running}"
                   Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}"
                   Foreground="{StaticResource WarningBrush}" Margin="0,0,0,12" />

        <!-- Check Results -->
        <ItemsControl ItemsSource="{Binding Checks}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Style="{StaticResource CardStyle}" Margin="0,0,0,8" Padding="16,12">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>

                            <!-- Pass/Fail Icon -->
                            <TextBlock Grid.Column="0" Grid.RowSpan="3"
                                       FontSize="20" VerticalAlignment="Top"
                                       Margin="0,0,12,0">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Text" Value="⚠" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Passed}" Value="True">
                                                <Setter Property="Text" Value="✔" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>

                            <!-- Name + Level -->
                            <StackPanel Grid.Column="1" Grid.Row="0" Orientation="Horizontal">
                                <TextBlock Text="{Binding Name}" FontWeight="SemiBold"
                                           FontSize="14" Foreground="{StaticResource TextBrush}" />
                                <Border CornerRadius="4" Padding="8,2" Margin="10,0,0,0"
                                        VerticalAlignment="Center">
                                    <TextBlock Text="{Binding LevelLabel}" FontSize="11" FontWeight="SemiBold"
                                               Foreground="White" />
                                    <Border.Background>
                                        <SolidColorBrush Color="{Binding LevelBrushKey, Converter={StaticResource BrushKeyToColorConverter}, FallbackValue=#555555}" />
                                    </Border.Background>
                                </Border>
                            </StackPanel>

                            <!-- Description -->
                            <TextBlock Grid.Column="1" Grid.Row="1"
                                       Text="{Binding Description}"
                                       Foreground="{StaticResource TextBrush}" Opacity="0.7"
                                       FontSize="12" Margin="0,4,0,0" TextWrapping="Wrap" />

                            <!-- Message -->
                            <TextBlock Grid.Column="1" Grid.Row="2"
                                       Text="{Binding Message}"
                                       Foreground="{StaticResource TextBrush}"
                                       FontSize="12" Margin="0,6,0,0" TextWrapping="Wrap" />

                            <!-- Remediation -->
                            <TextBlock Grid.Column="1" Grid.Row="3"
                                       Text="{Binding Remediation}"
                                       Foreground="{StaticResource WarningBrush}"
                                       FontSize="12" Margin="0,4,0,0" TextWrapping="Wrap">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Setter Property="Visibility" Value="Collapsed" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Remediation}" Value="">
                                                <Setter Property="Visibility" Value="Collapsed" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</UserControl>
